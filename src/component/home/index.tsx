import React from 'react';

interface IProps {}
interface IState {}

const Home = (props: IProps, state: IState) => {
  return (
    <div>
      <h2>Home Page</h2>
    </div>
  );
};

export default Home;
